id: BLACKSTONE_UNDERGROUND
type: PALETTE


sampler: &sampler
  dimensions: 3
  type: EXPRESSION
  expression: |
    if(y > max, -1,
    if(y < min,  1,
    -y + min + (offset(x,y,z)+1)/2*(max+1-min)
    ))
  variables:
    min: $meta.yml:blackstone-bottom
    max: $meta.yml:blackstone-top
  samplers:
    offset:
      dimensions: 3
      type: OPEN_SIMPLEX_2
      frequency: 0.08

layers:
  - materials:
      - minecraft:deepslate: 1
      - minecraft:blackstone: 1
    layers: 1
    sampler: *sampler
